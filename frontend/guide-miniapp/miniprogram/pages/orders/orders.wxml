<view class="page">
  <view class="tabs">
    <text
      wx:for="{{tabs}}"
      wx:key="key"
      class="tab {{activeTab === item.key ? 'active' : ''}}"
      data-key="{{item.key}}"
      bindtap="switchTab"
    >{{item.label}}</text>
  </view>

  <view wx:if="{{loading}}" class="muted center">加载中…</view>
  <view wx:elif="{{!list.length}}" class="muted center">暂无订单</view>

  <view wx:for="{{list}}" wx:key="id" class="card" data-id="{{item.id}}" bindtap="goDetail">
    <view class="row">
      <text class="title">{{item.sessionTitle}}</text>
      <text class="status status-{{item.status}}">{{item.statusLabel}}</text>
    </view>
    <view class="row">
      <text class="muted">{{item.date}} {{item.timeRange}}</text>
      <text class="muted">{{item.peopleCount}} 人</text>
    </view>
    <view class="no">{{item.orderNo}}</view>
  </view>
</view>
